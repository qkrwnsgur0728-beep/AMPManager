<UserControl x:Class="AMPManager.View.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:oxy="http://oxyplot.org/wpf" 
             mc:Ignorable="d" 
             d:DesignHeight="720" d:DesignWidth="1280"
             Background="{StaticResource AppBackground}">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="1.5*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource PanelStyle}" Margin="0,0,5,0">
                <Grid>
                    <Image Source="{Binding CameraImage1}" Stretch="Uniform"/>

                    <TextBlock Text="CAM 1 (OFF)" 
                               Foreground="{StaticResource SecondaryText}" 
                               FontSize="32" FontWeight="SemiBold"
                               HorizontalAlignment="Center" VerticalAlignment="Center">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding CameraImage1}" Value="{x:Null}">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>

                    <Button Content="TEST(1)" Command="{Binding TestCommand}" 
                            HorizontalAlignment="Left" VerticalAlignment="Top" 
                            Width="70" Height="30" Margin="10"
                            Background="#FF5252" Foreground="White" FontWeight="Bold" BorderThickness="0">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="5"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </Grid>
            </Border>

            <Border Grid.Column="1" Style="{StaticResource PanelStyle}" Margin="5,0,0,0">
                <Grid>
                    <Image Source="{Binding CameraImage2}" Stretch="Uniform"/>
                    <TextBlock Text="CAM 2 (OFF)" 
                               Foreground="{StaticResource SecondaryText}" 
                               FontSize="32" FontWeight="SemiBold"
                               HorizontalAlignment="Center" VerticalAlignment="Center">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding CameraImage2}" Value="{x:Null}">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Grid>
            </Border>
        </Grid>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource PanelStyle}" Margin="0,0,5,0">
                <Grid Margin="20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="불량률 현황" Grid.Row="0" Foreground="{StaticResource PrimaryText}" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>

                    <Grid Grid.Row="1" Margin="0,0,0,20">
                        <ProgressBar Value="{Binding DefectRate}" Maximum="100" Height="20" Background="#404050" Foreground="#FF5252" BorderThickness="0"/>
                    </Grid>

                    <Border Grid.Row="2" Background="#3A3A4A" CornerRadius="8" Padding="15">
                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                            <TextBlock Text="불량 / 전체" Foreground="{StaticResource SecondaryText}" FontSize="14" HorizontalAlignment="Center" Margin="0,0,0,10"/>

                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <TextBlock Text="{Binding DefectCount}" FontSize="36" FontWeight="Bold" Foreground="#FF5252"/>
                                <TextBlock Text=" / " FontSize="28" FontWeight="Light" Foreground="{StaticResource SecondaryText}" VerticalAlignment="Bottom" Margin="8,0,8,6"/>
                                <TextBlock Text="{Binding CurrentComplete}" FontSize="36" FontWeight="Bold" Foreground="White"/>
                            </StackPanel>

                            <Separator Background="#505060" Margin="10,20"/>

                            <TextBlock Text="{Binding DefectRate, StringFormat='불량률 {0:F1}%'}" 
                                       FontSize="20" Foreground="{StaticResource PrimaryText}" FontWeight="SemiBold" 
                                       HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>

            <Border Grid.Column="1" Style="{StaticResource PanelStyle}" Margin="5,0,0,0">
                <Grid>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,15,20,0" Panel.ZIndex="1">
                        <Rectangle Width="12" Height="12" Fill="#00C1D4" RadiusX="2" RadiusY="2" Margin="0,0,5,0" VerticalAlignment="Center"/>
                        <TextBlock Text="검사량" Foreground="#E0E0E0" FontSize="12" Margin="0,0,15,0" VerticalAlignment="Center"/>

                        <Rectangle Width="12" Height="12" Fill="#FF5252" RadiusX="2" RadiusY="2" Margin="0,0,5,0" VerticalAlignment="Center"/>
                        <TextBlock Text="불량 개수" Foreground="#E0E0E0" FontSize="12" VerticalAlignment="Center"/>
                    </StackPanel>

                    <oxy:PlotView Model="{Binding CombinedChartModel}" Background="Transparent" Margin="15,40,15,10"/>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>