<UserControl x:Class="AMPManager.View.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="800"
             Background="{StaticResource AppBackground}">

    <Grid>
        <Border Width="360" Height="420" Style="{StaticResource PanelStyle}" 
                Background="#2F2F3D" VerticalAlignment="Center" HorizontalAlignment="Center">
            <StackPanel VerticalAlignment="Center" Margin="30">

                <TextBlock Text="AMP Manager" Foreground="{StaticResource AccentColor}" 
                           FontSize="28" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                <TextBlock Text="시스템 로그인" Foreground="{StaticResource SecondaryText}" 
                           FontSize="14" HorizontalAlignment="Center" Margin="0,0,0,40"/>

                <TextBlock Text="ID" Foreground="{StaticResource PrimaryText}" Margin="0,0,0,5"/>
                <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" 
                         Height="40" VerticalContentAlignment="Center" Padding="10,0"
                         Background="#3A3A4A" Foreground="White" BorderThickness="0" Margin="0,0,0,20">
                    <TextBox.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="4"/>
                        </Style>
                    </TextBox.Resources>
                </TextBox>

                <TextBlock Text="PASSWORD" Foreground="{StaticResource PrimaryText}" Margin="0,0,0,5"/>
                <PasswordBox x:Name="txtPassword"
                             Height="40" VerticalContentAlignment="Center" Padding="10,0"
                             Background="#3A3A4A" Foreground="White" BorderThickness="0" Margin="0,0,0,20">
                    <PasswordBox.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="4"/>
                        </Style>
                    </PasswordBox.Resources>
                </PasswordBox>

                <TextBlock Text="{Binding ErrorMessage}" Foreground="#FF5252" 
                           FontSize="12" Height="20" HorizontalAlignment="Center" Margin="0,0,0,10"/>

                <Button Content="로그인" 
                        Command="{Binding LoginCommand}" 
                        CommandParameter="{Binding ElementName=txtPassword}"
                        Height="50" Background="{StaticResource AccentColor}" 
                        Foreground="White" FontWeight="Bold" FontSize="16" BorderThickness="0">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="4"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>