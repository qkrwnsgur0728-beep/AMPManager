<UserControl x:Class="AMPManager.View.LogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource AppBackground}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource PanelStyle}" Margin="0,0,0,15" Padding="15">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="날짜" VerticalAlignment="Center" Foreground="{StaticResource SecondaryText}" Margin="0,0,10,0"/>

                <DatePicker SelectedDate="{Binding SearchDate}" 
                            Width="120" Height="30" 
                            VerticalContentAlignment="Center"/>

                <CheckBox Content="정상" IsChecked="{Binding IsCheckedNormal}" Foreground="{StaticResource PrimaryText}" VerticalAlignment="Center" Margin="30,0,10,0" Style="{StaticResource {x:Type CheckBox}}"/>
                <CheckBox Content="불량" IsChecked="{Binding IsCheckedDefect}" Foreground="#FF5252" VerticalAlignment="Center" Margin="10,0,0,0"/>

                <Button Content="조회" Command="{Binding SearchCommand}" Width="80" Height="30" Margin="30,0,0,0" Background="{StaticResource AccentColor}" Foreground="White" FontWeight="Bold" BorderThickness="0">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="4"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Style="{StaticResource PanelStyle}">
            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="40"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="TIMESTAMP" FontWeight="Bold" Foreground="{StaticResource SecondaryText}" VerticalAlignment="Center"/>
                    <TextBlock Grid.Column="1" Text="제품명 (Product)" FontWeight="Bold" Foreground="{StaticResource SecondaryText}" VerticalAlignment="Center"/>
                    <TextBlock Grid.Column="2" Text="판정" FontWeight="Bold" Foreground="{StaticResource SecondaryText}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    <TextBlock Grid.Column="3" Text="상세" FontWeight="Bold" Foreground="{StaticResource SecondaryText}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Grid>
                <Separator Grid.Row="1" Background="#4A4A5A" Margin="0,0,0,5"/>

                <DataGrid Grid.Row="2" ItemsSource="{Binding LogData}" AutoGenerateColumns="False" HeadersVisibility="None" Background="Transparent" BorderThickness="0" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#404050" RowBackground="Transparent" RowHeight="45" SelectionMode="Single" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Binding="{Binding Timestamp}" Width="2*" Foreground="{StaticResource PrimaryText}"/>
                        <DataGridTextColumn Binding="{Binding PropertyName}" Width="2*" Foreground="{StaticResource SecondaryText}"/>

                        <DataGridTextColumn Binding="{Binding Status}" Width="1*">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                    <Setter Property="Foreground" Value="{StaticResource AccentColor}"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Status}" Value="불량">
                                            <Setter Property="Foreground" Value="#FF5252"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTemplateColumn Width="1*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="보기" Command="{Binding DataContext.OpenDetailCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Width="50" Height="26" Background="#3A3A4A" Foreground="White" BorderThickness="0">
                                        <Button.Resources>
                                            <Style TargetType="Border">
                                                <Setter Property="CornerRadius" Value="4"/>
                                            </Style>
                                        </Button.Resources>
                                    </Button>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>