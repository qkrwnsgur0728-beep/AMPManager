<Window x:Class="AMPManager.View.LogDetailWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        Title="검사 상세 정보" Height="900" Width="1600"
        WindowStartupLocation="CenterOwner"
        Background="#1E1E2D">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0" Background="#2F2F3D" CornerRadius="5" Margin="5">
                <Grid>
                    <TextBlock Text="Top View (Cam 1)" Foreground="#808080" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    <Image Source="{Binding Img1}" Stretch="Uniform"/>
                </Grid>
            </Border>
            <Border Grid.Column="1" Background="#2F2F3D" CornerRadius="5" Margin="5">
                <Grid>
                    <TextBlock Text="Side View (Cam 2)" Foreground="#808080" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    <Image Source="{Binding Img2}" Stretch="Uniform"/>
                </Grid>
            </Border>
        </Grid>

        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="#2F2F3D" CornerRadius="10" Margin="5" Padding="10">
                <Grid>
                    <TextBlock Text="형상 분석 (Shape)" Foreground="#00C1D4" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Top"/>

                    <lvc:CartesianChart Series="{Binding ShapeSeriesCollection}" 
                                        VisualElements="{Binding ShapeVisuals}" Margin="0,25,0,0" LegendLocation="Bottom" Hoverable="False" DataTooltip="{x:Null}">

                        <lvc:CartesianChart.AxisX>
                            <lvc:Axis ShowLabels="True" MinValue="-150" MaxValue="150">
                                <lvc:Axis.Separator>
                                    <lvc:Separator Stroke="#333333" StrokeThickness="1"/>
                                </lvc:Axis.Separator>
                            </lvc:Axis>
                        </lvc:CartesianChart.AxisX>

                        <lvc:CartesianChart.AxisY>
                            <lvc:Axis ShowLabels="True" MinValue="-150" MaxValue="150">
                                <lvc:Axis.Separator>
                                    <lvc:Separator Stroke="#333333" StrokeThickness="1"/>
                                </lvc:Axis.Separator>
                            </lvc:Axis>
                        </lvc:CartesianChart.AxisY>

                    </lvc:CartesianChart>
                </Grid>
            </Border>

            <Border Grid.Column="1" Background="#2F2F3D" CornerRadius="10" Margin="5" Padding="10">
                <Grid>
                    <TextBlock Text="편차 프로파일 (Deviation)" Foreground="#00C1D4" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Top"/>
                    <lvc:CartesianChart Series="{Binding DeviationSeriesCollection}" Margin="0,25,0,0" LegendLocation="Bottom">
                        <lvc:CartesianChart.AxisX>
                            <lvc:Axis Labels="{Binding DeviationLabels}" ShowLabels="False">
                                <lvc:Axis.Separator>
                                    <lvc:Separator Step="1" StrokeThickness="0"/>
                                </lvc:Axis.Separator>
                            </lvc:Axis>
                        </lvc:CartesianChart.AxisX>
                        <lvc:CartesianChart.AxisY>
                            <lvc:Axis ShowLabels="True" Sections="{Binding DeviationSections}">
                                <lvc:Axis.Separator>
                                    <lvc:Separator Stroke="#333333" StrokeThickness="1" StrokeDashArray="2"/>
                                </lvc:Axis.Separator>
                            </lvc:Axis>
                        </lvc:CartesianChart.AxisY>
                    </lvc:CartesianChart>
                </Grid>
            </Border>

            <Border Grid.Column="2" Background="#2F2F3D" CornerRadius="10" Margin="5" Padding="10">
                <Grid>
                    <TextBlock Text="동심도 (Concentricity)" Foreground="#00C1D4" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Top"/>
                    <lvc:CartesianChart Series="{Binding ConcentricitySeriesCollection}" Margin="0,25,0,0" LegendLocation="Bottom" Hoverable="False" DataTooltip="{x:Null}">
                        <lvc:CartesianChart.AxisX>
                            <lvc:Axis ShowLabels="True" MinValue="-20" MaxValue="20">
                                <lvc:Axis.Separator>
                                    <lvc:Separator Stroke="#333333" StrokeThickness="1" Step="5"/>
                                </lvc:Axis.Separator>
                            </lvc:Axis>
                        </lvc:CartesianChart.AxisX>
                        <lvc:CartesianChart.AxisY>
                            <lvc:Axis ShowLabels="True" MinValue="-20" MaxValue="20">
                                <lvc:Axis.Separator>
                                    <lvc:Separator Stroke="#333333" StrokeThickness="1" Step="5"/>
                                </lvc:Axis.Separator>
                            </lvc:Axis>
                        </lvc:CartesianChart.AxisY>
                    </lvc:CartesianChart>
                </Grid>
            </Border>
        </Grid>

        <Border Grid.Row="2" Background="#2F2F3D" CornerRadius="10" Padding="20" Margin="0,0,0,10">
            <StackPanel>
                <TextBlock Text="검사 상세 리포트" Foreground="#00C1D4" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="30"/>
                        <RowDefinition Height="30"/>
                    </Grid.RowDefinitions>

                    <Grid.Resources>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="#B0B0B0"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                        </Style>
                        <Style x:Key="Val" TargetType="TextBlock">
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="Margin" Value="10,0,0,0"/>
                        </Style>
                    </Grid.Resources>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="일시"/>
                    <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Timestamp}" Style="{StaticResource Val}"/>
                    <TextBlock Grid.Row="1" Grid.Column="0" Text="제품명"/>
                    <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding PropertyName}" Style="{StaticResource Val}"/>

                    <TextBlock Grid.Row="0" Grid.Column="2" Text="판정 결과"/>
                    <TextBlock Grid.Row="0" Grid.Column="3" Text="{Binding Status}" Style="{StaticResource Val}" Foreground="{Binding StatusColor}"/>
                    <TextBlock Grid.Row="1" Grid.Column="2" Text="불량 사유"/>
                    <TextBlock Grid.Row="1" Grid.Column="3" Text="{Binding DefectReason}" Style="{StaticResource Val}"/>

                    <TextBlock Grid.Row="0" Grid.Column="4" Text="Hole Offset"/>
                    <TextBlock Grid.Row="0" Grid.Column="5" Text="{Binding HoleOffset, StringFormat={}{0:F3} px}" Style="{StaticResource Val}"/>
                    <TextBlock Grid.Row="1" Grid.Column="4" Text="Area Size"/>
                    <TextBlock Grid.Row="1" Grid.Column="5" Text="{Binding AreaSize, StringFormat={}{0:F2} px²}" Style="{StaticResource Val}"/>
                </Grid>
            </StackPanel>
        </Border>

        <Button Grid.Row="3" Content="닫기" IsCancel="True" Height="40" Width="120" Background="#4A4A5A" Foreground="White" FontWeight="Bold" Margin="0,5,0,0">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}" CornerRadius="5">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Button.Template>
        </Button>
    </Grid>
</Window>